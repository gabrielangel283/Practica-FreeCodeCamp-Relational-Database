// Para reiniciar el servicio
sudo service postgresql start

// Conectarse a la bd
psql --username=freecodecamp --dbname=postgres

// Usar la bd en el scriot bash
PSQL="psql --username=freecodecamp --dbname=<database_name> -t --no-align -c"


// Hacer un backup de la bd
pg_dump -cC --inserts -U freecodecamp number_guess > number_guess.sql


// Salvar el backup en el proyecto
psql -U postgres < number_guess.sql



// script bd
create table users(
  user_id serial primary key,
  user_name varchar(22) not null unique
);

create table games(
  game_id serial primary key,
  guesses int not null default 0,
  user_id int not null references users(user_id)
);

insert into users(user_name) values('gabriel'),('marcelo');

insert into games(guesses,user_id) values(6,1),(8,1),(4,1),(12,2);








